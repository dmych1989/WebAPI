# WebAPI Dependencies
# Core (required)
fastapi>=0.115
uvicorn>=0.34
pydantic>=2.10
pydantic-settings>=2.7
PyYAML>=6.0
httpx>=0.28
aiohttp>=3.10

# Pydantic settings extra
python-dotenv>=1.0

# Testing
pytest>=8.3
pytest-asyncio>=0.25

# Optional: browser automation
# playwright>=1.50

# Optional: improved token counting
# tiktoken>=0.8

# Optional: SSE (fastapi built-in StreamingResponse is sufficient)
# sse-starlette>=2.0